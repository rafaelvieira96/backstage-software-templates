replicaCount: 1

image:
  repository: rafaelvieiramartinsteste/${{values.app_name}}
  pullPolicy: IfNotPresent
  # tag: ${{values.app_name}}-${{values.app_env}}-latest
  tag: latest

resources:
  requests:
    cpu: 40m
    memory: 40M